name: Native Host Conformance

on:
  push:
    branches: [main]
    paths:
      - 'desktop/**'
      - 'packages/engine/**'
      - 'contracts/**'
      - 'docs/contracts/**'
      - '.github/workflows/native-host-conformance.yml'
  pull_request:
    branches: [main]
    paths:
      - 'desktop/**'
      - 'packages/engine/**'
      - 'contracts/**'
      - 'docs/contracts/**'
      - '.github/workflows/native-host-conformance.yml'

permissions:
  contents: read

jobs:
  rust-native-host-conformance:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.16.0

      - name: Use Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwayland-dev libgtk-3-dev

      - name: Run native-host conformance gate
        run: pnpm -C packages/engine run conformance:native-host
